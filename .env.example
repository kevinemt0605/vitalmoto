# Firebase Web config (create a file named .env in project root with these values)
# Fill these with the values from Firebase Console → Project settings → Your apps (Web app)
# Keep this file private and DO NOT commit it to a public repo.
REACT_APP_FIREBASE_API_KEY=your_api_key # e.g. AIzaSy... (string)
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com # e.g. myproj.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id # e.g. myproj-12345
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project.appspot.com # e.g. myproj-12345.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id # numeric string
REACT_APP_FIREBASE_APP_ID=your_app_id # e.g. 1:123456:web:abcdef

# Optional: If you plan to run server-side admin tasks locally, place a
# `serviceAccountKey.json` in the project root (NOT in the frontend or public folder)
# and add its path here for local server reference. Do NOT commit the JSON to git.
# SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# Notes:
# - For the frontend React app you only need the REACT_APP_FIREBASE_* web config above.
# - The service account JSON is only used server-side (Cloud Functions or your own server)
#   and should never be embedded in the browser app.
